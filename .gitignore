# dependencies
node_modules/

# build output (regenerate with `bun run build` and scripts/build-vendors.sh)
dist/
bench/vendor/

# runtime noise
*.log
.DS_Store

# agent-local state (skill discovery symlinks, design history); the shared skill lives in skills/
.zcode/
.agent-workbench/
.npmrc
